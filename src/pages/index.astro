---
/**
 * The US landing page, and the site's x-default.
 *
 * Everything lives in Landing.astro and markets.ts — this file exists so the
 * route does. See src/lib/markets.ts for why the UK gets its own URL rather
 * than a runtime word-swap on this one.
 */
import Landing from '../components/Landing.astro';
import { US } from '../lib/markets';
---
<Landing market={US} />
