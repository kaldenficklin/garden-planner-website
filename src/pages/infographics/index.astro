---
import InfographicIndex from '../../components/InfographicIndex.astro';
import { publishedPosts, infographics } from '../../lib/posts';

const posts = infographics(await publishedPosts(), 'en');
---
<InfographicIndex posts={posts} lang="en" />
